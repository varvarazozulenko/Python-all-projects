# Проект #2: Анализ текстовых данных на Flask

Проект является логическим продолжением предыдущего и посвящен созданию простейшего веб-приложения на микрофреймворке `Flask`.

Подразумевается, что будет переиспользована логика (юз-кейсы) из предыдущего проекта, а использование `Flask` ограничится 
реализацией тонкого слоя взаимодействия с пользователем, альтернативы консольному режиму из предыдущего проекта.*

Контрольная точка предполагает два варианта: I для номеров с 1 по 12 по списку, II вариант для последующих.

Оба варианта делают функционал главной страницы и статистики. **I вариант** помимо нее реализует топ-слова, **II вариант** - облако слов.

## Структура проекта

```sh
.
├── src/
│   ├── venv/
│   └── python_kt_2/
|       ├── cli/
|           └── __init__.py
|           ├── commands.py
|           ├── parse_params.py
│           └── renderer.py
|       ├── core/
|           └── __init__.py
|           ├── exceptions.py
|           ├── preprocess.py
|           ├── stopwords.txt
|           ├── tokenize.py
│           └── types.py
|       ├── corpus/
|           └── ...
|       ├── use_cases/
│           ├── __init__.py
|           ├── stats.py
|           ├── top_words.py
│           └── word_cloud.py
|       ├── web/
│           ├── static/ 
│               └── ...
│           ├── templates/
│               ├── _layout.html
|               ├── index.html
|               ├── stats.html
│               └── topwords.html
│           ├── app.py
|           └── __init__.py
|       └── __init__.py
├── requirements.txt
└── setup.py
```

## Сценарии использования и критерии оценивания

### Сценарий использования "Главная страница" (1 балл)

| Номер | Действие | Результат |
|---|---|---|
| 1 | Пользователь переходит по корневому адресу | Отображается стартовая страница с ссылками на функциональные страницы |
| 2 | Пользователь выбирает интересующий функционал ("Статистика", "Топ-слова", "Облако слов") | Переход по ссылке |

### Сценарий использования "Статистика" (4 балла)

| Номер | Действие                                                                | Результат                                                       |
|-------|-------------------------------------------------------------------------|-----------------------------------------------------------------|
| 1     | Пользователь переходит по адресу страницы `/stats`                      | Отображается страница с формой для выбора файла и опции `pos`   |
| 2     | Пользователь выбирает файл для обработки из числа текстовых (0.5 балла) | Становится доступна отправка формы (0.5 балла)                  |
| 3     | Пользователь отправляет форму                                           | Обработка формы, проверка формата файла (0.5 балла)             |
| 4.A   | ...удалось обработать файл                                              | Отображение статистики в табличном виде (2 балла)               |
| 4.B   | ...не удалось обработать файл                                           | Сообщение-подсказка об ошибке, возврат к пункту `2` (0.5 балла) |

### Сценарий использования "Топ-слова" (7 баллов)

| Номер | Действие                                                                | Результат                                                                             |
|-------|-------------------------------------------------------------------------|---------------------------------------------------------------------------------------|
| 1     | Пользователь переходит по адресу страницы `/top`                        | Отображается страница с формой для выбора файла и режима нормализации (1 балл)        |
| 2     | Пользователь выбирает файл для обработки из числа текстовых (0.5 балла) | Становится доступна отправка формы (0.5 балла)                                        |
| 3     | Пользователь отправляет форму                                           | Получение данных (0.5 балла), проверка формата (0.5 балла) и размера файла (0.5 балла) |
| 4.A   | ...удалось обработать файл                                              | Отображение топ-слов в табличном виде  (3 балла)                                      |
| 4.B   | ...не удалось обработать файл                                           | Сообщение-подсказка об ошибке, возврат к пункту `2` (0.5 балла)                       |

### Сценарий использования  "Облако слов" (7 баллов)

| Номер | Действие | Результат                                                                                             |
|---|---|-------------------------------------------------------------------------------------------------------|
| 1 | Пользователь переходит по адресу страницы `/cloud` | Отображается страница с формой для выбора файла и режима предобработки (базовый либо полный) (1 балл) |
| 2     | Пользователь выбирает файл для обработки из числа текстовых (0.5 балла) | Становится доступна отправка формы (0.5 балла)                                                        |
| 3     | Пользователь отправляет форму                                           | Получение данных (0.5 балла), проверка формата (0.5 балла) и размера файла (0.5 балла)                |
| 4.A   | ...удалось обработать файл                       | Отображение изображения с облаком слов (3 балла)                                                      |
| 4.B   | ...не удалось обработать файл                    | Сообщение-подсказка об ошибке, возврат к пункту `2` (0.5 балла)                                       |

### Бонусы и штрафы

**Бонусы**: 

- стилизация компонентов интерфейса (например, через `daisy ui`): до двух баллов

**Штрафы**:
- минус балл при несоблюдении норм форматирования и наличии ошибок линтера
- минус балл за несоответствие рекомендованной структуре

## Настройка окружения и запуск

### Создание виртуального окружения

**Windows**

```sh
python -m venv venv
```

**Mac / Unix**

```sh
python3 -m venv venv
```

### Активация виртуального окружения

**Windows**

```sh
venv\Scripts\activate
```

**Mac / Unix**

```sh
source venv\bin\activate
```

### Добавление зависимостей

В зависимости от наличия доступов к `pypi.org`:

- либо устанавливаем через `pip install -r requirements.txt`

- либо копируем вручную содержимое `site-packages` в соответствующую директорию внутри `venv`

### Билд / установка пакета

```sh
pip install -e .
```

...либо, при наличии ошибок

```sh
py -m pip install -e .
```

### Запуск приложения

```sh
web
```

### Работа в системе контроля версий

Создайте ветку со своей фамилией

```sh
git switch -c фамилия-измени-меня
```

Далее фиксируйте изменения по мере продвижения

```sh
git add .
git commit -m "работа над заданием"
```

Если гит выдает ошибку, почитайте и погуглите её: могут быть не настроены имя и почта, может отсутствовать сам гит.

Если всё ок, залейте вашу ветку на гитхаб:

```
git push -u origin фамилия-измени-меня
```

Далее можете делать (или не делать) промежуточные коммиты.

**Главное, не забудьте закоммитить и запушить финальные изменения!**
